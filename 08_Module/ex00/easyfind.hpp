#ifndef EASYFIND_HPP
# define EASYFIND_HPP

# include <iostream>
# include <string>
# include <vector>
# include <list>

template <typename T>
void easyfind( T &container, int number )
{
	return (std::find(stack.begin(), stack.end(), number));
}

#endif